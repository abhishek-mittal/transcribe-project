{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main window. The sidecar is invoked via Rust commands that already have shell access via the plugin; the JS layer does not need shell:allow-execute.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "core:webview:default",
    "core:app:default",
    "core:menu:default",
    "dialog:default",
    "fs:default"
  ]
}