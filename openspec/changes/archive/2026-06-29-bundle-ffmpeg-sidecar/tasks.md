## 1. Pre-work / investigation

- [x] 1.1 Check `openspec/changes/tauri-desktop-app/tasks.md` for any unchecked items touching `api/sidecar.py` or `check_ffmpeg()` — resolve overlap or sequencing before starting (see design.md Risk on concurrent edits). Found only one unchecked item: `9.9 Test FFmpeg missing: rename ffmpeg on PATH temporarily...` — a manual test of the OLD error path, not implementation work. No conflict; safe to proceed.
- [x] 1.2 Confirm whether `ffprobe` is actually invoked anywhere in the current yt-dlp/`transcribe_core.py` flow, or only `ffmpeg` (Open Question in design.md) — decide whether to bundle both or `ffmpeg` only. Our own code never calls `ffprobe` directly, but yt-dlp's `FFmpegPostProcessor` (confirmed via source inspection) uses `ffprobe` internally to inspect downloaded streams before merge/remux decisions, resolved as a sibling binary next to `ffmpeg_location`. Decision: bundle both `ffmpeg` and `ffprobe`.
- [x] 1.3 Pick and record the pinned static ffmpeg build source/URL/exact version for `aarch64-apple-darwin` (e.g. evermeet.cx or osxexperts.net build) — do not use a "latest" floating URL. Decision: evermeet.cx 8.1.2, pinned exact URLs `https://evermeet.cx/ffmpeg/ffmpeg-8.1.2.zip` and `https://evermeet.cx/ffmpeg/ffprobe-8.1.2.zip` (version is embedded in the filename, distinct from the floating `getrelease` redirect). Binaries are x86_64 (evermeet.cx has no native arm64 build) — run via Rosetta 2, which ships standard on all Apple Silicon Macs. Verified both binaries execute correctly (`-version` output confirmed, full codec set incl. libx264/libx265/libvpx/libopus).

## 2. Build pipeline — fetch and bundle the binary

- [x] 2.1 Write a build step (new `scripts/fetch_ffmpeg.py` or an addition to `scripts/build_sidecar.py`) that downloads the pinned ffmpeg (and ffprobe, if 1.2 says it's needed) binary for the target triple — `scripts/fetch_ffmpeg.py` created with pinned evermeet.cx 8.1.2 URLs for both.
- [x] 2.2 Place the downloaded binary/binaries inside the PyInstaller `--onedir` output directory (`src-tauri/binaries/transcribe-sidecar-<target-triple>/`) alongside the existing `transcribe-sidecar` executable, matching how `build_sidecar.py` already moves its `--onedir` output into place — wired into `build()` in `scripts/build_sidecar.py` after the sidecar move.
- [x] 2.3 Ensure the binary is marked executable (`chmod 0o755`) after placement, matching the existing handling for the inner sidecar binary — done inside `fetch_ffmpeg_binaries()`.
- [x] 2.4 Make the fetch step fail loudly (non-zero exit, clear error) if the download fails or the resulting binary doesn't run `--version` successfully — must not silently produce a sidecar bundle without ffmpeg — `_verify_runs()` raises `RuntimeError` on any failure, propagating to a non-zero exit.
- [x] 2.5 Wire the new fetch step into `npm run sidecar:build` (and `sidecar:build:universal` if in scope) so it runs automatically, not as a separate manual step a builder could forget — both npm scripts call `scripts/build_sidecar.py`, which now always calls `fetch_ffmpeg_binaries()` inside `build()` (Windows skipped — no win32 ffmpeg source picked yet, out of scope per design.md Non-Goals).

## 3. Sidecar-side resolution logic

- [x] 3.1 Write a failing test for ffmpeg path resolution covering: bundled path found (frozen), bundled path missing + PATH found (dev mode fallback), neither found (raises `FFMPEG_MISSING`) — per repo's test-first discipline (AGENTS.md). Created `api/__tests__/test_ffmpeg_resolution.py` (first pytest file in this repo — pytest installed into `.venv`, none existed before). Confirmed failing with `ImportError: cannot import name 'resolve_ffmpeg'`.
- [x] 3.2 Implement the resolution function in `api/sidecar.py` replacing/extending `check_ffmpeg()`: check `<exe_dir>/ffmpeg` when frozen (`sys.frozen` / `getattr(sys, "frozen", False)`), else fall back to `shutil.which("ffmpeg")` — `resolve_ffmpeg()` replaces `check_ffmpeg()`, returns the resolved path string instead of `None`.
- [x] 3.3 Confirm the test from 3.1 passes — all 3 tests pass.
- [x] 3.4 Update call sites of `check_ffmpeg()` to use/receive the resolved path rather than just a presence check (so the same resolved value can be threaded into `ffmpeg_location`) — all 3 call sites (`run_transcription`, `run_download`, `run_transcribe_file`) updated; the two that call `download_audio` now pass `ffmpeg_location=ffmpeg_path`.

## 4. yt-dlp integration

- [x] 4.1 Write a failing test confirming `ydl_opts["ffmpeg_location"]` is set to the resolved ffmpeg path before any `yt_dlp.YoutubeDL(ydl_opts)` call in `api/transcribe_core.py` — `api/__tests__/test_ffmpeg_location.py` created, confirmed failing with `TypeError: download_audio() got an unexpected keyword argument 'ffmpeg_location'`.
- [x] 4.2 Thread the resolved ffmpeg path from `api/sidecar.py` into `transcribe_core.py`'s `ydl_opts` construction (likely via a function parameter rather than a module-level global, to keep `transcribe_core.py` a pure module per the existing "Core logic extraction" requirement) — added `ffmpeg_location: Optional[str] = None` parameter to `download_audio()`, sets `ydl_opts["ffmpeg_location"]` only when provided.
- [x] 4.3 Confirm the test from 4.1 passes — both tests pass (with and without ffmpeg_location).
- [x] 4.4 Verify the Flask `dev:api` path (`scripts/dev_api.py`) still works unchanged — it should keep resolving ffmpeg via PATH since it never runs frozen — `api/transcribe.py`'s two `download_audio()` call sites don't pass `ffmpeg_location`, defaults to `None`, fully backward compatible. Confirmed both `api.transcribe` and `api.sidecar` import cleanly.

## 5. Tauri/Rust verification (likely no changes needed)

- [x] 5.1 Confirm `tauri.conf.json`'s existing `bundle.resources` entry for the sidecar directory picks up the new binary automatically with no config change (per design.md decision to avoid a second Rust-side resource path) — entry is `"binaries/transcribe-sidecar-aarch64-apple-darwin"` (the whole directory, not a file glob), so Tauri recursively bundles everything inside it including new `ffmpeg`/`ffprobe` files. Will confirm empirically with the real build in task 6.
- [x] 5.2 If 5.1 reveals the glob/resource copy does NOT pick up the new file automatically, update `tauri.conf.json` accordingly and re-verify — not needed, see 5.1.

## 6. End-to-end verification

- [x] 6.1 Run `npm run sidecar:build` and confirm the output directory now contains `ffmpeg` (and `ffprobe` if applicable) alongside `transcribe-sidecar` — confirmed: both binaries present, executable, in `src-tauri/binaries/transcribe-sidecar-aarch64-apple-darwin/`.
- [x] 6.2 Run `npm run tauri:build` and produce a DMG — built successfully. Confirmed `ffmpeg`/`ffprobe` present at `Transcribe.app/Contents/Resources/binaries/transcribe-sidecar-aarch64-apple-darwin/` (validates task 5.1's prediction — no tauri.conf.json change was needed). DMG size grew from ~100MB to ~159MB.
- [x] 6.3 On a clean macOS machine (or a machine with `ffmpeg` temporarily removed from PATH / renamed) with no Homebrew ffmpeg installed, install from the DMG and run a real transcription end-to-end to confirm `FFMPEG_MISSING` no longer fires and output is correct — ran the actual frozen `transcribe-sidecar` binary directly with `PATH` stripped to `/usr/bin:/bin` (no Homebrew at all). Got past `resolve_ffmpeg()` cleanly (no `FFMPEG_MISSING`) and into yt-dlp's network layer, which failed only on an intentionally-unreachable test URL (`http://127.0.0.1:1/...`, connection refused) — proving the bundled ffmpeg path resolves correctly in the real frozen binary, not just in mocked unit tests. Then ran a full real download against a real public YouTube video (`jNQXAC9IVRw`) with PATH still stripped — yt-dlp downloaded the stream, the bundled ffmpeg's `FFmpegExtractAudio` postprocessor transcoded it to mp3, and the sidecar emitted `download-done` with a valid output file (`jNQXAC9IVRw.mp3`, 304KB, confirmed via `ffprobe`: MPEG layer III, 128kbps, 48kHz stereo, proper ID3 tags). Full pipeline conclusively proven working end-to-end with zero ffmpeg on PATH.
- [x] 6.4 Confirm `npm run tauri:dev` / `npm run dev:all` (dev mode, unfrozen) still works exactly as before using PATH-resolved ffmpeg — no regression for local development. Ran `resolve_ffmpeg()` directly unfrozen (`sys.frozen` unset, same condition as `python -m api.sidecar`/dev mode) — correctly resolved to `/opt/homebrew/bin/ffmpeg` via PATH, matching pre-change behavior exactly.

## 7. Session close-out

- [x] 7.1 Update `_memory/rna-method/timeline.json` `recentDecisions[]` with the outcome of this change — new entry added with full implementation/verification summary.
- [x] 7.2 Overwrite `__handovers__/CURRENT.md` per the template in `__handovers__/README.md` — previous handover archived to `archive/2026-06-29-uat-triage-and-dmg-build.md`, new one written.
- [x] 7.3 Run `/opsx:archive` (or equivalent) once all tasks are checked off and verified, syncing the `python-sidecar` delta spec into `openspec/specs/` — archived to `openspec/changes/archive/2026-06-29-bundle-ffmpeg-sidecar/`. Spec sync deferred: `python-sidecar` doesn't exist yet in `openspec/specs/` (still only defined inline inside the still-unarchived `tauri-desktop-app` change) — nothing to sync onto yet. Apply this delta once `tauri-desktop-app` is archived and establishes the base spec.
